Execute this task.

Return ONLY one `builder_result` JSON object matching `builder_result.schema.json`.

TASK_JSON:
{{TASK_JSON}}

Constraints:
- allowed_globs={{ALLOWED_GLOBS}}
- forbidden_globs={{FORBIDDEN_GLOBS}}
- allow_new_files={{ALLOW_NEW_FILES}}
- allow_lockfile_changes={{ALLOW_LOCKFILE_CHANGES}}
- diff_limits=files<= {{MAX_FILES_TOUCHED}}, lines<= {{MAX_LINES_CHANGED}}
- autonomy_policy:
{{AUTONOMY_POLICY}}
- If `task_kind` is `verify_only` or `question`, do not modify files.
- Do not invent verification results; runner executes verification separately.
