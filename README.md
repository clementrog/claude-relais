# Relais

Deterministic orchestration runner for Claude Code: **Brain → Hands → Judge**.

This repo is the implementation of the roadmap in `ROADMAP.json` and the current execution plan in `docs/NEW-PLAN.md`.

## Docs

- Roadmap: `ROADMAP.json`
- Current plan: `docs/NEW-PLAN.md`

## Notes

- `pilot/` is legacy documentation for older workflows.
